{#
########################################################
    site: 404
########################################################
#}

{# layout settings #}
{% extends "_layouts/_master.twig" %}

{# block: content #}
{% block content %}
    {% cache if not craft.config.devMode %}

    <section class="wrapper">
        <h1>Seite nicht gefunden</h1>
        <p>Die Seite, welche Sie aufgerufen haben existiert leider nicht (mehr).</p>
        <p><a href="{{ siteUrl }}" alt="{{ siteName }}">Zur Startseite</a></p>
    </section>

    {% endcache %}
{% endblock %}
